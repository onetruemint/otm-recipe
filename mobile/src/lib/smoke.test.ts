// Placeholder so CI has a real Jest run. Phase 2's shared modules
// (quantities, time formatting, etc., see docs/recipe-app-mvp-plan.md
// Section 9) will add real coverage.
describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
